source 'https://rubygems.org'
gem 'rspec', '>= 2.12'
gem 'rake', '>= 0.9.2.2'

group :optional do
  # Only needed if using Natural Assertions
  gem 'sorcerer'
end

group :dev do
  gem 'bluecloth'
  gem 'ghpreview'
end
